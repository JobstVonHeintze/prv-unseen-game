# Sprint review — F-01 Part I walking skeleton and tester desk

- **Date:** 2026-09-20
- **Feature:** F-01
- **Plan:** `docs/plans/2026-09-20-part-i-walking-skeleton.md`
- **Review context:** same-session builder (not an independent host session)
- **Verify:** `bash scripts/smoke.sh`
- **Boundaries:** `harness/boundaries.report.json` (strict=0, all classes 0)

## User behavior

A human plays Part I gates 1–7 in the Player against real canon; a tester
flags a scene; an author inspects the run and attaches a storyboard prompt
in the Console; the drifter reaches every Part I gate by date.

## Rubric

| Category | Score | Notes |
|----------|-------|-------|
| Correctness | 2 | Player evening loop, flags, capture→s01, Console findings + prompt history, drifter g1–g7, progress trigger before day 16. |
| Verification | 2 | `pnpm test` (22) and `bash scripts/smoke.sh` recorded as exact evidence refs. |
| Scope discipline | 2 | Stayed on F-01. No proposals, no real renderer, no hosted playtest. |
| Reliability | 2 | `bash init.sh` exits 0. Smoke restarts API + Vite on loopback. |
| Maintainability | 1 | Walking-skeleton UIs are thin; Player/Console share no design system beyond tokens. Enough to play and flag. |
| Handoff readiness | 2 | Plan, this review, `claude-progress.md`, feature evidence. |

**Total: 11 / 12 — Accept**

A 2 on maintainability would be a later desk pass (F-02): proposal diffs,
entity edit, less raw JSON in the inspector. Not in F-01 scope.

## Grounding

- **Changed behavior:** playable Part I slice + tester desk. Spec:
  `specs/001-part-i-walking-skeleton/spec.md`.
- **Proof:** `pnpm test`; `bash scripts/smoke.sh`; contract files under
  `tests/contract/` (gate-reach, capture-use, hidden-state, witness, rewind,
  player-surface, console-desk, determinism).
- **Scope bound by?** F-01 P0s in the plan. Out of scope left out: proposals,
  LLM render, sandbox commit.
- **Next session reads:** `claude-progress.md`, this review, the plan at its
  stable path.
- **What would fail if this were false?** Drifter test, smoke HTML greps,
  hidden-state allowlist, Blanche witness, Honest rewind.

## Machine evidence

- Tests: 14 files / 22 passed.
- Smoke: API `127.0.0.1`, Player `UNSEEN`, Console `contrejour`, gate-reach.
- Boundaries: `harness/boundaries.report.json` — `peer_coupling: 0`. Player
  view file is `packages/engine/src/views/player-view.ts` so the ratchet does
  not false-flag `engine → player`.

## Carry-forward (not F-01 blockers)

- F-02: canon proposals with diff + approval.
- Hand-written scenes still dump Markdown; Player strips the leading heading only.
- JSON canon index (F-01 exception vs SQLite).
- No commit on this tree (`commit: ask`; repo has no first commit yet).
