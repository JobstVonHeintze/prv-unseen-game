# Sprint review — F-05 Console entity editor

- **Date:** 2026-09-20
- **Feature:** F-05
- **Plan:** `docs/plans/2026-09-20-entity-editor.md`
- **Review context:** same-session builder (not an independent host session)
- **Durable host session:** N/A
- **Verify:** `bash scripts/smoke.sh`
- **Boundaries:** `harness/boundaries.report.json` (status `checked`, strict=0, all classes 0)

## User behavior

An author searches canon, opens an entity as YAML, previews a diff and
validation report, then queues a proposal; nothing writes until approve;
Player never sees the catalogue.

## Rubric

| Category | Score | Notes |
|----------|-------|-------|
| Correctness | 2 | Search finds Tilde + VIP scene; source is the file; preview does not write or store; queue is the existing POST. |
| Verification | 2 | Exact evidence refs: entity-editor contract, `bash scripts/smoke.sh`. 21/37 suite green. Browser: search, preview, queue, reject; YAML unchanged. |
| Scope discipline | 2 | No structured forms, no AI author, no create-id. F-04 tree not adopted as this feature. |
| Reliability | 2 | Tests copy canon. Live desk rejected the check so `scene.tilde-vip.yaml` still has the original beat. |
| Maintainability | 2 | Path from id prefix; preview before `/:id` matcher. |
| Handoff readiness | 2 | Plan Status done, this review, feature evidence, `claude-progress.md`. |

**Total: 12 / 12 — Accept**

## Summary

- Plans reviewed: 1
- Complete: 1 | Partial: 0 | Changed: 0 | Deferred: 0
- Contract tests: 37 passing / 37 total
- Module boundaries: status checked; reason N/A; strict=0
  `{policy_gap:0, misplaced_contract:0, infra_leak:0, peer_coupling:0}`.
  Evidence: harness/boundaries.report.json

## Plan-by-plan assessment

### Console entity editor with proposal preview

**Status:** Complete
**Deliverables:** 0–5 present.
**Module Boundaries:** Clean (`checked`, strict 0). See harness/boundaries.report.json
**Legacy Removal:** Path/YAML submit remains as a fallback, not a second write path.
**Carry-Forward:** structured forms later; rehearsal still 404.

## Grounding

- **Changed behavior:** Console catalogue + preview. Spec: prototype-design §6.
- **Proof:** `pnpm exec vitest run tests/contract/entity-editor.test.ts --reporter=dot`;
  `bash scripts/smoke.sh`. Browser: `tilde` lists `char.tilde`,
  `scene.tilde-vip`, `secret.s-tilde-vip`; source opens YAML; preview shows
  unified diff + valid; queue pending; reject left the file as
  `A locked room off the white cube.`
- **Scope bound by?** F-05 P0s.
- **Next session reads:** `claude-progress.md`, this review, the plan at its
  stable path.
- **What would fail if this were false?** Preview writing YAML; `preview`
  treated as a proposal id; Player `/v1/runs/{id}/canon/search` returning 200;
  search missing `scene.tilde-vip`.
- **Fixtures:** source from `readCanonFile`; preview from `previewProposal`.
- **Live tests:** none that need credentials.
- **Policy tables:** validator runs on a merged canon copy, not a mocked table.
- **Generated catalogs:** search labels are id/name/beat, not fallback prose.

## Machine evidence

- Tests: 21 files / 37 passed.
- Smoke: `smoke ok` at 2026-09-20T21:49:23Z.
- Boundaries: `harness/boundaries.report.json` — status `checked`,
  strict `violations_total` 0, `peer_coupling: 0`.

## Copilot PR comments

No open PR at review time. Open Copilot comments: 0.

## Carry-forward (not F-05 blockers)

- Structured entity forms (later desk).
- Rehearsal queue still 404.
- Commit/push of F-04 + F-05 when the owner asks (`commit: ask`).
